# Spike Results 022：错误引用到污染治理入口

## 结论

问答引用的最小治理入口已接入。

已实现：

- 问答引用列表展示“标记污染”按钮。
- 点击后复用已有 `/api/sources/quarantine`。
- 被隔离源资料会从普通搜索、向量检索和图谱中排除。

## 更新文件

```text
apps/web/public/js/qa-view.js
apps/web/public/main.js
```

## 当前限制

- 目前按引用的 `source_id` 隔离整个源资料。
- 还不能只标记单个文本片段。
- 还没有记录“用户为什么标记污染”的原因。

## 下一步

继续补删除范围：

- 仅隔离源资料。
- 删除源文件。
- 删除源文件 + 派生产物。
- 删除后保留审计记录。
